"""telegram_service.py — optional, failure-isolated

send_incident(), is_configured(). A Telegram failure must never destroy an incident. Dashboard stays authoritative.
"""

# TODO: implement during build phase (one file per turn).
