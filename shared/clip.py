"""clip.py — clips from search results

shot.generate_stream() for one topic; video.generate_stream([(s,e),...]) for a multi-topic reel; resp.results.compile().
"""

# TODO: implement (one file per turn).
